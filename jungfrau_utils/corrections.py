def apply_gain_pede(*_args, **_kwargs):
    raise AttributeError(
        "The function `apply_gain_pede` was removed in jungfrau_utils/1.0. "
        "Use `jungfrau_utils.JFDataHandler` to perform gain/pedestal corrections."
    )


def apply_geometry(*_args, **_kwargs):
    raise AttributeError(
        "The function `apply_geometry` was removed in jungfrau_utils/1.0. "
        "Use `jungfrau_utils.JFDataHandler` to perform geometry corrections."
    )
